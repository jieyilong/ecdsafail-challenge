# 2026-06-05 KAL_FOLD_CARRY_TRUNC_W 24->23 + tail nonce 3155 (PROMOTED)

Base = promoted `1664274` (robertkodra, cb72): 1390 q × 1,531,871 T = 2,129,300,690.
Submission `cade2d0` PROMOTED: 1390 q × **1,531,353 T = 2,128,580,670** (-720,020).

## What worked

One orthogonal truncation lever on the cb72 K=2 raw-PA route:
- `KAL_FOLD_CARRY_TRUNC_W` 24 -> 23 (only the FOLD window; DOUBLE stays 24).
  -518 avg executed Toffoli, peak-neutral at 1390 q. Value-exact on the
  reachable support (the dropped fold carry bit is 0 there); failures are pure
  Fiat-Shamir island, dodged by the tail nonce.
- `DIALOG_TAIL_NONCE` re-found to **3155** (clean 0/0/0 over 9024).

## Density reality on the cb72 base (measured via build+eval at the base nonce)

The cb72 base is at the edge of every gentle lever (classical mismatches at the
inherited nonce): slope1005=13, margin8=16, applyc19=11, active258=10,
kal_d23=9, **kal_f23=6** (gentlest). Uniform body-carry trims break thousands
(no exact slack on a tight base); bandtrim late-only is exact but 0 savings.
kal_f23's island was found at nonce 3155 after ~3200 nonces of full-validation
search (~7.5 min on 11 threads) — density ~1/3000 for the 6-break lever.

## Reusable tool

`src/bin/island_search_jac.rs` — full-validation tail-nonce island searcher.
Bit-exact with eval_circuit (startup self-check vs affine k*G; reproduces known
islands). ~7 nonce/s on 11 threads (sim-bound, NOT derivation-bound on the K=2
route, so the Jacobian comb barely helps vs island_search_fast — the per-nonce
cost is the 10.24M-op simulation with early-exit, not the k*G derivation).
Usage: `ISLAND_LOWQ=1 <KNOB=val> ISLAND_THREADS=11 ./target/release/island_search_jac <start> <count> [step]`

## What to try next

- The remaining single-notch levers on this base break 9-16 inputs -> islands
  at ~e^-(breaks) density (1e-4 .. 1e-7), needing 50k-1M+ nonces at 7 nonce/s.
  Competitive solvers (robertkodra cb72 island at nonce 7.3e13) clearly use big
  remote compute. A faithful classical width/compare pre-filter (re-implement
  the truncated raw-tobitvector GCD on dx = target_x-offset_x mod p, reject
  nonces with any width/compare overflow before full sim) would make these
  tractable locally, but it must bit-exactly model active_width(step) +
  compare_bits + apply truncations together. Not built this session.
